#ifndef COMMAND_H
#define COMMAND_H

#include "boolean.h"

/* berisi command-command yang akan digunakan */
boolean move();

boolean pick_up();

boolean drop_off();

boolean map();

boolean to_do();

boolean in_progress();

boolean buy();

boolean inventory();

boolean help();

#endif